<div class="top-bar">
    <div class='menu align-left'>
        <%= render 'home/links' %>
        <li>

            <%= link_to fa_icon('chevron-left'), bids_path %>
        </li>
        <li>
            <%= link_to fa_icon('chevron-right'), students_path %>

        </li>
    </div>

    <div class="menu align-right">
        <%= render 'drop-form', school: @school %>
        <%= render 'search' %>
    </div>

</div>

<div class="row align-center small-3">
    <%= render 'table' %>
</div>

<p id="notice"><%= notice %></p>

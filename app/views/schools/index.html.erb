<div class="container">

    <div class="top-bar">
        <div>
            <%= link_to fa_icon('home'), '/' %>
            <%= link_to fa_icon('book'), '/index' %>
            <%= link_to fa_icon('chevron-left'), services_path %>
            <%= link_to fa_icon('chevron-right'), students_path %>
        </div>

        <div class="top-bar-right">
            <ul class="menu">
                <%= render 'search' %>
            </ul>
        </div>
    </div>

    <h1 class="row align-center">Schools</h1>

    <div class="row align-center small-3">

        <%= render 'table' %>
    </div>

    <div class="row align-center">
        <%= render 'form', school: @school %>
    </div>

    <p id="notice"><%= notice %></p>
</div>
